#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main(){
    // double x;
    // scanf("%lf", &x);
    // int answer = ceil(x);
    // int answer = floor(x);
    // int answer = round(x);

    // double x2;
    // scanf("%lf", &x2);
    // double answer = sqrt(x2);
    // printf("%0.2lf\n", answer);

    // int a,b;
    // scanf("%d %d", &a,&b);
    // int answer = pow(a*1.0,b*1.0);
    // printf("%d\n", answer);

    int x;
    scanf("%d", &x);
    int answer = abs(x);
    printf("%d\n", answer);

for(int i = 1; i < 1000; i++){
    printf("%d /n", i);
}
    return 0;
}